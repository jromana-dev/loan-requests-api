# loan-requests-api
Simple REST API built with Spring Boot for managing personal loan requests. It allows clients to create loan applications, update their status following a defined workflow, and retrieve existing requests. Data is stored in memory for simplicity.
